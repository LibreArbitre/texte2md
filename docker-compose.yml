services:
  texte2md:
    build: .
    container_name: texte2md
    ports:
      - "3000:3000"
    restart: unless-stopped
    init: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
    environment:
      - NODE_ENV=production
